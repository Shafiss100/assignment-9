import React from 'react';

const Blogs = () => {
    return (
        <div>
            <h1>1. What is Context api ?</h1>
            <p>context help to pass any data without depend on any props. Props connect all the component each other. but some time we need to pass a data without props. then we use the react context. in that case if we use props for passing the data, we cannot pass the data in the spacific component.  </p>
            <br />
            <h1>2. What is symbol tag ?</h1>
            <p>symbol tag is use in html. for some spacigic icon like copyright icon.... the symbol tag help to use some icon by taxt. farther we need to need help another website for the icon </p>
            <br />
            <h1>3. What is inline blog elements ?</h1>
            <p>inline blok is a kind of a container. where we can set height and width value. Displays an element as an inline element. Any height and width properties will have no effect.</p>
        </div>
    );
};

export default Blogs;